var assert = require('assert');
describe('getHouseInConStraint', function() {
  describe('HouseService.getHouseInConStraint', function() {
    it('It should filter the house under budget', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});